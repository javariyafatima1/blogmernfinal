import React from 'react';
import Writeupdate from './Writeupdate';

const CreateBlogs = ({title}) => {
  return (
    <div>
      <Writeupdate titlename={title}/>
    </div>
  );
}

export default CreateBlogs;
